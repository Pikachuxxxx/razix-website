import React from "react";
import ReactMarkdown from 'react-markdown'
import {Text} from '@chakra-ui/react';

const ReleaseNotes = () => {
return (

        <ReactMarkdown color='#ffffff'>
        </ReactMarkdown>
    );
}

export default ReleaseNotes;
